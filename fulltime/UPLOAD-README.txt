Teesside 1+2 static page upload files

Upload this whole folder as one directory, keeping the relative file structure unchanged:
- index.html
- styles.css
- app.js
- data.js
- assets/

If embedding this page inside another site path, keep index.html in the same folder as styles.css/app.js/data.js, because the page uses relative paths such as ./styles.css and ./assets/...
